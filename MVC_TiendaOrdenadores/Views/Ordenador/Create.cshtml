@model MVC_TiendaOrdenadores.Models.Ordenador

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Ordenador</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ComponentesLIst![0].Id" class="control-label">Procesador</label>
                <select asp-for="ComponentesLIst![0].Id" class="form-control" asp-items="@(new SelectList(ViewBag.Procesadores, "Id", "Serie"))">
                    <option value="">Seleccionar Procesador</option>
                </select>
                <span asp-validation-for="ComponentesLIst![0].Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ComponentesLIst![1].Id" class="control-label">Procesador</label>
                <select asp-for="ComponentesLIst![1].Id" class="form-control" asp-items="@(new SelectList(ViewBag.Memorias, "Id", "Serie"))">
                    <option value="">Seleccionar Memoria</option>
                </select>
                <span asp-validation-for="ComponentesLIst![1].Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ComponentesLIst![2].Id" class="control-label">Procesador</label>
                <select asp-for="ComponentesLIst![2].Id" class="form-control" asp-items="@(new SelectList(ViewBag.DiscosDuros, "Id", "Serie"))">
                    <option value="">Seleccionar Disco Duro</option>
                </select>
                <span asp-validation-for="ComponentesLIst![2].Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
